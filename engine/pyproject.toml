[project]
name = "duty-engine"
version = "0.1.0"
description = "A股值班台确定性引擎：无 LLM，唯一写库者"
requires-python = "==3.11.*"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/duty_engine"]
